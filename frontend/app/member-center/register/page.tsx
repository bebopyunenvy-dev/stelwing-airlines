// 此為註冊頁面🔹1️⃣ /register（註冊）

// 後端的工作：接收使用者註冊資料 → 驗證格式 → 雜湊密碼 → 存入資料庫。

// 前端要做的：
// 做一個「註冊頁面」（像 member-center/register/page.tsx），
// 表單填 email / password / name → 點送出 → 用 fetch('/api/auth/register') 傳到後端。